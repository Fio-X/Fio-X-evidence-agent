#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {renderPublication} from '../runtime/web/publication.mjs';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const out=path.join(ROOT,'outputs','v19-browser');fs.mkdirSync(out,{recursive:true});
const x=Array.from({length:120},(_,i)=>i/10),y=x.map(v=>Math.sin(v));
const spec={schema_version:'0.1.0',title:'WebGL qualification probe',dek:'This fixture must fail closed when the host browser has no WebGL context.',reader_question:'Does browser QA detect a requested GPU rendering path that the host cannot initialize?',visual_thesis:'A missing WebGL runtime must become a capability failure rather than a visually empty PASS.',story_graph_ref:'fixtures/v19-browser/webgl-story.json',infographic_plan_ref:'fixtures/v19-browser/webgl-infographic.json',delivery:{mode:'html',self_contained:true,static_fallback:'png',breakpoints:[390,1024],max_initial_bytes:8000000},modules:[{id:'probe-stat',type:'stat',value:'120',unit:'points',title:'Probe points',story_node_ids:['probe'],explanatory_dimension:'context',width:'third'},{id:'webgl-probe',type:'plotly',engine:'plotly',visual_type:'scattergl',title:'WebGL probe',story_node_ids:['probe'],explanatory_dimension:'distribution',width:'wide',spec:{visual_type:'scattergl',figure:{data:[{type:'scattergl',mode:'markers',x,y}],layout:{title:{text:'WebGL probe'}}}}}]};
const rendered=renderPublication(spec);fs.writeFileSync(path.join(out,'webgl-unqualified-publication.json'),JSON.stringify(spec,null,2)+'\n');fs.writeFileSync(path.join(out,'webgl-unqualified.html'),rendered.html);console.log(JSON.stringify({status:'BUILT_EXPECTED_FAIL',bytes:rendered.manifest.initial_bytes}));
