#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {renderPublication} from '../runtime/web/publication.mjs';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const out=path.join(ROOT,'outputs','v19-browser');fs.mkdirSync(out,{recursive:true});
const spec={schema_version:'0.1.0',title:'D3 runtime qualification probe',dek:'This fixture must fail closed when the pinned D3 browser runtime is absent.',reader_question:'Does browser QA reject an advanced D3 module whose executable runtime is unavailable?',visual_thesis:'An unavailable advanced renderer must produce a blocked-module failure rather than silent visual substitution.',story_graph_ref:'fixtures/v19-browser/d3-story.json',infographic_plan_ref:'fixtures/v19-browser/d3-infographic.json',delivery:{mode:'html',self_contained:true,static_fallback:'png',breakpoints:[390,1024],max_initial_bytes:2000000},modules:[{id:'probe-stat',type:'stat',value:'8',unit:'nodes',title:'Fixture nodes',story_node_ids:['probe'],explanatory_dimension:'context',width:'third'},{id:'d3-probe',type:'d3',engine:'d3',visual_type:'radial_network',title:'D3 radial network probe',story_node_ids:['probe'],explanatory_dimension:'network',width:'wide',spec:{visual_type:'radial_network',data:{nodes:[{id:'a'},{id:'b'}],edges:[{source:'a',target:'b'}]}}}]};
const rendered=renderPublication(spec);fs.writeFileSync(path.join(out,'d3-unqualified-publication.json'),JSON.stringify(spec,null,2)+'\n');fs.writeFileSync(path.join(out,'d3-unqualified.html'),rendered.html);console.log(JSON.stringify({status:'BUILT_EXPECTED_FAIL',assets:rendered.manifest.runtime_assets}));
