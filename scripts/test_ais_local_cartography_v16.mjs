#!/usr/bin/env node
import assert from 'node:assert/strict';import {readFile,mkdir,writeFile} from 'node:fs/promises';
import {BASEMAP_CONTENT_HASH,BASEMAP_ID,BASEMAP_LICENSE,BASEMAP_SOURCE_URL} from '../runtime/pi/cartography.mjs';import {lintVizSpec,renderVizBundle,critiqueViz} from '../runtime/pi/viz.mjs';
const r=JSON.parse(await readFile(new URL('../fixtures/v16-movement/ais-syros-sampled.json',import.meta.url),'utf8')),claim='claim-ais-syros-v16',rows=[{source:'first retained AIS point',target:'last retained AIS point',value:1,trajectory_points:JSON.stringify(r.points)}];
const spec={schema_version:'1.1.0',reader_task:'spatial',visual_family:'spatial',data_topology:'geo_edges',chart_type:'cartographic_flow_map',title:'At harbour scale, the trajectory outgrows the basemap',subtitle:'Selected AIS positions from a public Syros test dataset',alt:'Local observed AIS path around Syros rendered against the project 1:110m Natural Earth basemap. The trajectory is visible but the basemap lacks harbour-scale coastline and infrastructure detail.',source_note:'ITSLab-UAegean/vesseltrack-tools sample dataset, upstream blob 417b6779fd8c417f9e554c6a5c13a040d57a30d4; README states sample is based on real vessel movement around Syros, Greece.',note:'This is a deliberate cartographic stress test. The 1:110m Natural Earth country basemap is too coarse for harbour-scale interpretation; no berth, quay, channel or local coastline claim is made.',claim_id:claim,sql:'fixture:ais-syros-sampled.json',unit:'trajectory',source_field:'source',target_field:'target',value_field:'value',geometry_semantics:'observed_trajectory',geometry_crs:'EPSG:4326',projection:'natural_earth_1',basemap_id:BASEMAP_ID,basemap_source_url:BASEMAP_SOURCE_URL,basemap_license:BASEMAP_LICENSE,basemap_content_hash:BASEMAP_CONTENT_HASH,route_provenance_note:'ITSLab-UAegean/vesseltrack-tools sample, selected exact AIS rows from real-movement-based Syros test data',trajectory_points_field:'trajectory_points',extent_mode:'data',extent_padding_ratio:.2,reference_path:'none',locator_inset:true,aggregation_policy:'none',annotations:[]};
const lint=lintVizSpec(spec,rows,{verified_claim_ids:[claim]});assert.equal(lint.passed,true,lint.blockers.join(' | '));const b=renderVizBundle(spec,rows),c=critiqueViz(spec,rows,lint,b.desktop);assert.equal(c.passed,true,JSON.stringify(c));assert.equal(c.issues.some(i=>i.code==='basemap_detail_mismatch'),true,JSON.stringify(c));const out=new URL('../outputs/v16-ais/',import.meta.url);await mkdir(out,{recursive:true});await writeFile(new URL('syros-local-trajectory.svg',out),b.desktop);await writeFile(new URL('syros-local-trajectory.mobile.svg',out),b.mobile);console.log(`AIS local cartography diagnostic PASS critic=${c.score}`);
