# QGIS publication templates

Only templates rendered and visually approved inside `viz-qgis` may be marked `READY`. Placeholder template IDs remain `PENDING_QGIS_RENDER_VALIDATION`; the router must not treat them as production templates.
