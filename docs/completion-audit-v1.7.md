# v1.7 RC Completion Audit

Release candidate: `1.7.0-rc1`

## Implemented

- MeasureSemantics 1.0 normalization and deterministic comparability rules.
- ClaimSpec 1.0 normalization, derived metrics, and editorial grammar planning.
- VisualRecipe 2.0 optional semantic fields with JSON-schema validation.
- Visual compiler semantic blocking before backend routing.
- Main Pi newsroom planning gate for semantic declaration, comparability, and strong grammar/chart mismatches.
- Backend policy 3.0 topology/artifact/capability hard filtering before scoring.
- Generated Pi copies of the canonical semantic modules and Rust runtime materialization.
- Semantic contract fingerprinting now includes normalized measure semantics and ClaimSpec when declared.
- 20-case semantic adversarial corpus, 7 grammar cases, 6 backend matrices.
- 2026-09-14 Saudi/Hormuz offline cold-story regression.

## Verified in this environment

Focused tests pass for semantic contracts, VisualRecipe schema/normalization, editorial semantics, adversarial qualification, cold-story replay, backend router, evidence router, Pi backend planning, GIS compiler, graph backends, ECharts, render cache, and Python publication backend.

The visual compiler smoke suite passed through the environment time limit and the remaining commands were run from the exact interruption point; all remaining commands passed.

## External / not claimed by this RC

- live `news investigate` with the pinned Rust control plane, Pi CLI, DuckDB recomputation and real provider credentials;
- multi-provider human preference qualification;
- full R/QGIS/PyGMT/Datashader/Web execution on this host;
- the final 10-story networked cold-story release gate.

These remain required before promoting `1.7.0-rc1` to a final `1.7.0` release.
