{
  "platform": "macos",
  "principle": "local-first when semantics are equivalent; specialized backends remain authoritative when they add required capability",
  "schema_version": "1.0.0",
  "tools": {
    "artifact_inventory": {
      "fallback": "portable_runtime",
      "macos_candidates": ["mdfind", "mdls", "file"],
      "policy": "local_first",
      "rationale": "Safe native primitives can reduce startup or I/O overhead while preserving the high-level tool contract."
    },
    "download_data": {
      "fallback": "portable_runtime",
      "macos_candidates": ["curl"],
      "policy": "local_first",
      "rationale": "Safe native primitives can reduce startup or I/O overhead while preserving the high-level tool contract."
    },
    "duckdb_query": {
      "fallback": "duckdb",
      "macos_candidates": ["sqlite3"],
      "policy": "specialized",
      "rationale": "The specialized backend provides semantics or formats that a generic system utility cannot replace safely."
    },
    "fetch_url": {
      "fallback": "portable_runtime",
      "macos_candidates": ["curl"],
      "policy": "local_first",
      "rationale": "Safe native primitives can reduce startup or I/O overhead while preserving the high-level tool contract."
    },
    "news_search": {
      "fallback": "portable_runtime",
      "macos_candidates": ["curl"],
      "policy": "portable_network",
      "rationale": "Network discovery remains provider/API-specific; curl is available for compatible HTTP transport paths."
    },
    "newsroom_asset_requirements": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_award_mode_status": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/meta tool contract."},
    "newsroom_basemap_prepare": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_chart": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_competition_preflight": {"fallback":"portable_runtime","macos_candidates":["qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_editorial_discovery": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_expert_preference_record": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_explainer_critic": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_explainer_lint": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_explainer_plan": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_explainer_render": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_graph_extraction_status": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the core/meta tool contract."},
    "newsroom_illustration_critic": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_illustration_generate": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_illustration_lint": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_illustration_plan": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_critic": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_lint": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_plan": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_preview": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_render": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_revise": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_infographic_vision_critic": {"fallback":"browser_or_model_runtime","macos_candidates":["qlmanage"],"policy":"specialized","rationale":"Native utilities may assist inspection, but browser/model semantics remain authoritative."},
    "newsroom_lieflat_catalog": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_lieflat_render": {"fallback":"portable_runtime","macos_candidates":["qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_map_validate": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/computation tool contract."},
    "newsroom_model_validate": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/computation tool contract."},
    "newsroom_network_analyze": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/computation tool contract."},
    "newsroom_network_reduce": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/computation tool contract."},
    "newsroom_parallel_tasks": {"fallback":"portable_runtime","macos_candidates":["mdfind","mdls","textutil","sips","sqlite3","curl","file","shasum"],"policy":"local_first","rationale":"Safe native primitives can reduce startup or I/O overhead while preserving the high-level tool contract."},
    "newsroom_portable_publication": {"fallback":"portable_runtime","macos_candidates":["qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_publication_plan": {"fallback":"portable_runtime","macos_candidates":["qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_publication_qa": {"fallback":"portable_runtime","macos_candidates":["qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_publication_render": {"fallback":"portable_runtime","macos_candidates":["qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_reference_patterns": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_semantic_novelty": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_story_graph": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_style_map": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_update_plan": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the core/planning tool contract."},
    "newsroom_visual_backend_plan": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the design/synthesis tool contract."},
    "newsroom_visual_backend_status": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the core/meta tool contract."},
    "newsroom_visual_concepts": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the synthesize/synthesis tool contract."},
    "newsroom_visual_skill": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the core/meta tool contract."},
    "newsroom_viz_critic": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_viz_lint": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_viz_plan": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "newsroom_viz_render": {"fallback":"portable_runtime","macos_candidates":["sips","qlmanage"],"policy":"portable","rationale":"Native tools can accelerate previews or metadata but do not replace deterministic render/QA contracts."},
    "record_claim": {"fallback":"portable_runtime","macos_candidates":[],"policy":"portable","rationale":"No safe macOS-native substitution is required for the verify/evidence tool contract."}
  }
}
